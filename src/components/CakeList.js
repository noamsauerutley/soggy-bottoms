import React, { Component } from 'react';
import CakeItem from './CakeItem'

const CakeList = (props) => {
  return (
    <div className="CakeList">
      { /* Render Cake Items Here*/ }
    </div>
  )
}

export default CakeList
